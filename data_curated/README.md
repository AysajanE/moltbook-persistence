# data_curated

Normalized, analysis-friendly tables derived from `data_raw/` (typically Parquet, partitioned by date).

- Keep out of git (see `.gitignore`).
- Prefer deterministic transformations and write lightweight schema manifests for audit.

