# data_features

Derived features used for modeling/estimation (thread event logs, thread metrics, agent time series, etc).

- Keep out of git (see `.gitignore`).
- Prefer recomputable artifacts over manual edits.

