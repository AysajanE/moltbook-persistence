# data_raw

Raw, immutable exports from upstream sources (Hugging Face, Moltbook API, Reddit, etc).

- Store data as retrieved (no normalization).
- Prefer timestamped snapshot folders and write an export manifest per run.
- Keep out of git (see `.gitignore`).

